<section>
    <img class="background" src="{{ settings.background | img_url: 'master' }}" alt="">
    <div class="title">
        <div class="main-title">{{ settings.header }}</div>
        <div class="sub-title">{{ settings.sub_header }}</div>
    </div>
</section>