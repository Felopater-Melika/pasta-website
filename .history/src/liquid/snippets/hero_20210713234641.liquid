<style>
    .background {
        height: 100vh;
        width: 100vw;
        background-image: url({{ settings.background | img_url: 'master' }});
    }
</style>
<section>
    <div class="background">
        <div class="main-title">{{ settings.header }}</div>
        <div class="sub-title">{{ settings.sub_header }}</div>
    </div>
</section>