<section class="main-product">
    <img src="{{ settings.mproduct.featured_image | img_url: 'master'
}}" alt="{{ settings.mproduct.featured_image.alt }}" class="main-product__image">
    <div class="main-product__title">{{ settings.mproduct.title }}</div>
    <div class="main-product__title">{{ settings.mproduct.description }}</div>
    <div class="product-title__price">{{ settings.mproduct.price | money }}</div>
    <div class="product-title__cart">Add To Cart</div>
</section>