{% render 'header' with { settings: section.settings }%} {% schema %} { "name": "Navigation", "settings": [ { "type": "url", "id": "first_link", "label": "First Link" } ], "presets": [ { "name": "Hero", "category": "Image w/ text" } ] } {% endschema %}