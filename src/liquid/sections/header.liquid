{% render 'header' with  {
  settings: section.settings
}%}

{% schema %}
  {
    "name": "Navigation",
    "settings": [
      {
        "type": "header",
        "content": "City name"
      }
    ],
    "presets": [
      {
        "name": "Hero",
        "category": "Image w/ text"
      }
    ]
  }
{% endschema %}